system_users:
  fst:
    name: Frank Strieter
    shell: /bin/bash
    groups:
      - docker
      - "{{ passwordless_sudo_group }}"
      - sudo
      - adm
    github: FrankStrieter
  backup:
    name: Backup
    groups:
      - "{{ passwordless_sudo_group }}"
      - sudo
      - adm